# Progress Summary

## July 31 — ouid/puid Numeric-to-String Cleanup

PM review found: `ouid=wh_config["org_id"]` etc — ouid/puid are string business IDs, but assigned from numeric `org["id"]`/`person["id"]`.

### Fixed files

| File | Issue | Fix |
|------|-------|-----|
| `agents/tdd/test_fire_newye_api.py` | `self.shu_org_id = org["id"]` used as `ouid` | Renamed to `self.shu_org_ouid`, stores `org["ouid"]`. `add_membership(person["id"], ...)` → `person["puid"]` |
| `agents/tdd/setup_fire_newye_campaign.py` | Same pattern + `find_or_create_person` bypassed API, returned no `puid` | Renamed attributes, stores `org["ouid"]`. `find_or_create_person` now inserts with `puid` and returns it |
| `src/routers/chat.py:48` | `ctx.get("ouid", str(ctx["organization_id"]))` — numeric fallback for ouid | Changed to `ctx["ouid"]` (guaranteed by `require_org_context`) |
| `AGENTS.md:44` | `ouid=1, puid=101` | `ouid=company, puid=zhangsan` |
| `CHAT_INTERFACE_FIX.md:11` | `"ouid": 10` | `"ouid": "shu"` |
| `docs/API.md:602-670` | 4 schema tables: `"puid": "int"` | `"puid": "string"`, `"ouid": "string"` |
| `README.md:137-140` | `from_ouid: 1, from_puid: 101` | `from_ouid: "shu"` etc |
| `agents/TDD_AGENT.md` | `ouid=1001`, `test_ouid=999` | `ouid="test_org"` |
| `docs/superpowers/...md:313` | `GET /resource?ouid=1` | `GET /resource?ouid=shop_a` |

### Verification

- `python3 -m compileall` on all modified files: ✅
- BE-01 (Seller Inventory): 8/8 passed ✅
- Three Kingdoms: 38 passed, 4 failed (pre-existing `UniqueViolation` on resource name, unrelated)
- Zero numeric ouid/puid in src/ or test scripts
