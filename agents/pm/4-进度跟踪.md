# 进度跟踪

**项目**：Uni-Resource Agent
**最后更新**：2026-08-04
**整体进度**：80%

---

## 里程碑

| 里程碑 | 目标日期 | 状态 |
| :--- | :--- | :--- |
| GitHub 仓库创建 | 7/15 | ✅ |
| 基础设施脚本就绪 | 7/20 | ✅ |
| 工具函数全部通过 | 7/25 | ✅ |
| Agent CPU模式跑通 | 7/28 | ✅ |
| 云资源审批确认 | 7/31 | ✅ |
| AMD GPU验证完成 | 8/3 | ✅ |
| 演示视频录制完成 | 8/5 | ✅ |
| 全部提交就绪 | 8/6 12:00 | ✅ |

---

## AUTH-02 / AUTH-03：认证与组织治理（2026-08-04）

| 任务 | 状态 | 说明 |
| :--- | :--- | :--- |
| AUTH-02 单账号多空间认证与注册 | ✅ | 基线 `a0b41d6`：register/login/seller-login alias/switch-organization 契约化，JWT 无 DB id，前端双模式，TDD 全绿 |
| AUTH-03 T1 DB 治理表 + 原子注册 + 治理函数 | ✅ | `space_invite`/`space_join_request` + `register_personal_space` + 9 治理函数（commit `4308f7b`） |
| AUTH-03 T2 Schema（移除 initial_ouid + 治理 schema） | ✅ | commit `41c954f`（extra=forbid → 422） |
| AUTH-03 T3 register 原子建 personal 空间 | ✅ | commit `3798ddf` + review `a4d1156`（空 name 422、并发重复 409） |
| AUTH-03 T4 空间治理端点 | ✅ | commit `5034f17` + review `ce01b34`（`create_org_with_owner` 原子、name/role/ouid 冲突校验） |
| AUTH-03 T5 收口 `POST /organizations/members` | ✅ | commit `484cc2f` + review `f469f7d`（无 DB id DTO、重复添加 409） |
| AUTH-03 T6 后端契约测试重写 | ✅ | commit `353cd0b` + `b133ac0`（修复 join-request 函数名遮蔽；37 passed） |
| AUTH-03 T7 旧测试迁移（9 文件） | ✅ | commit `357171d`（101 passed；setup 脚本成员改 DB 直插） |
| AUTH-03 T8 前端改造 | ✅ | commit `6efa557`（147 passed + vue-tsc build 成功） |
| AUTH-03 T9 测试文档（用户注册测试） | ✅ | commit `ff3b4e3` |
| AUTH-03 T10 全量验收 | ✅ | 红线自检通过（`initial_ouid` 零命中、`git diff --check` clean）；pytest 138 全绿（含 DB 超时重试）、前端 147 测试 + vue-tsc build 成功 |

## ORG-01：Workspace 治理与 Header 收口（2026-08-04）

> PM 规格书：`agents/pm/ORG-01_Workspace治理与Header收口产品规格书.md`

| 任务 | 状态 | 说明 |
| :--- | :--- | :--- |
| ORG-01 US-01..07 后端治理 | ✅ | AUTH-03 已交付（register personal 空间、建空间 owner、invite/join request/leave/kick/transfer、最后 owner 保护） |
| ORG-01 US-08 Header 收口 | ✅ | `AppHeader.vue` 删除 AI button/`navigate-ai` emit/`.btn` 样式；`App.vue` 删除 `onNavigateAi` 与 `@navigate-ai` 监听；AI 仅保留 Sidebar `Seller AI` 导航（commit 待提交） |
| ORG-01 TDD 补齐（AUTH014-017） | ✅ | `test_register_failure_leaves_no_half_created_account`、`test_cannot_invite_to_personal_space`、`test_cannot_join_request_personal_space`、`test_cannot_kick_personal_member`（422） |
| ORG-01 前端验证 | ✅ | 前端 144 tests + vue-tsc build 成功 |
| ORG-01 后端全量回归 | ✅ | pytest 142 全绿（含远程 DB 超时抖动重跑）；`header-ai-entry` 零命中 |

## FE-10：空间治理前端产品化收尾（2026-08-04）

> PM 规格书：`agents/pm/FE-10_空间治理前端产品化开发规格.md`
> 结论：ORG-01 的 Header 菜单只到占位页，商业观感不达标；比赛交付前必须加速补齐真实空间治理页面。

| 任务 | 状态 | 说明 |
| :--- | :--- | :--- |
| FE-10 T1 后端治理列表接口 | ✅ | `GET /spaces/current/members`、`GET /spaces/current/join-requests`、`GET /spaces/invites/mine`、`GET /spaces/join-requests/mine`、`POST /spaces/join-requests/reject`；`space_join_request.message` 列迁移；治理响应无 DB ID（`_GOV_FORBIDDEN`） |
| FE-10 T2 前端接线 | ✅ | `spaceGovernance.ts` API client；`SpaceMenu` action 到真实 view key；`App.vue` 治理 view 分支 + `onContextUpdated`/`onLeaveConfirmed`/`onNavigateSpace`；治理 key 不映射 `PlaceholderView` |
| FE-10 T3 治理页面 | ✅ | `SpaceManageView`（资料+成员表+邀请+复制 ouid）、`SpaceCreateView`（ouid 字符校验）、`JoinSpaceView`（邀请码输入+列表+申请双 tab）、`ReviewRequestsView`（非 owner/admin 无权限页）、`LeaveSpaceView`；`MembersTable`/`JoinRequestTable`/`InviteMemberModal`/`ConfirmDialog` |
| FE-10 T4 QA/视觉验收 | ✅ | 前端 210 tests（34 文件）+ vue-tsc build 成功；后端 test_auth_api 51 + test_spaces_api 8 全绿；四项安全扫描通过（仅测试断言/禁止字段集合/文档命中） |

## DEMO：双场景回归（2026-08-04）

> PM 计划：`agents/tdd/DEMO_双场景回归测试计划.md`
> 目标：测试库 Destroy & Rebuild → 双场景种子 → 冒烟回归 → 录屏提词器就绪。

| 任务 | 状态 | 说明 |
| :--- | :--- | :--- |
| DEMO-1 数据库核爆式清理 | ✅ | 停 uvicorn/vite；psql 本机缺失改用 psycopg2（连 postgres 维护库）执行 `DROP DATABASE unires WITH (FORCE)` + `CREATE DATABASE unires OWNER unires`；`init_db.py` 重建表与三国基数据；`test_org/demo_org/tmp_*` 零命中 |
| DEMO-2 双场景种子脚本 | ✅ | `scripts/seed_demo_data.py`：上下文 A `taobao_shop_a`（ecommerce，zhansan/lisi，诸葛亮联名羽扇 50 件 + 木牛流马模型 12 件，销售 5620/采购 4200/净现金流 1420/库存估值 1980）；上下文 B `xinye_campaign`（campaign，liubei/zhugeliang/zhansan，军粮 1000 石 + 箭矢 5000 支）；zhansan 单账号（读 `DEMO_ZHANSAN_PASSWORD`）；重复执行幂等（rerun 零 `[ok]`） |
| DEMO-3 冒烟测试 | ✅ | `agents/tdd/test_demo_smoke.py` 6 用例全绿：登录 JWT 仅业务字段、组织列表含双空间、switch 往返、卖家库存可见、火烧新野不见淘宝 SKU（+`/seller/stock` 403 隔离）、`/seller/chat` fake LLM 答“木牛流马模型 12 件”（数据来自真实 seeded DB） |
| DEMO-4 后端全量回归 | ✅ | auth/spaces/seller 套件 131 passed + 2 例网络超时重跑绿（远程 DB `Operation timed out`，非代码失败） |
| DEMO-5 安全扫描 + 前端构建 | ✅ | §7.2 四项扫描通过（仅字段名/测试断言命中）；`web npm run build` vue-tsc + vite 成功 |
| DEMO-DATA-01 录屏模拟数据增强 | ✅ | T-DATA：`seed_demo_data.py` 扩到 6 商品（WH-MAIN/WH-LIVE/WH-RETURN 三仓）、12 条流水、低库存 2 项（草船借箭 4 / 孔明灯香薰 5）、火烧新野 7 人员 + 5 实物 + 3 知识资源 + 6 条时间线（campaign_code=demo_xinye_recording）；重跑幂等 0 `[ok]`。T-TDD：`test_demo_smoke.py` 8 用例全绿（新增 summary low_stock、movements≥10、timeline≥6）。T-E2E：Playwright 5/5 有头全绿 + WebM 产出（含时间线/信息流物流人流断言，AI 锚点改草船借箭 4盒）。T-QA：四项安全扫描通过、`npm run build` 绿（132.15 kB）。清理重建后 DB 8 orgs 零残留，smoke 8/8 绿 |
| DEMO-DATA-02 全新录屏账号与数据 | ✅ | T-DATA：`seed_recording_data.py` 新建 `liuming`（`register_personal_space` 注册契约）+ `liuming_personal`(owner 默认) + `liuming_mingdeng_shop`(6 商品/3 仓/12 流水/低库存 2 项/销售收入 6410/采购 5946/库存估值 3582) + `liuming_xinye_review`(7 人员/7 资源/8 时间线/资金流 0)；密码只读 `DEMO_LIUMING_PASSWORD`（缺失 exit 1，不硬编码）；重跑幂等 0 `[ok]`。T-TDD：`test_recording_smoke.py` 10 用例全绿（9 必测 + fake-LLM chat：登录默认 personal、orgs 三空间、switch 仅 `{ouid}` 且带 DB id 422、personal/campaign seller 403、stock 6 行最低 `草船借箭纪念徽章` 3、summary 非零、timeline≥8 含信息/物流/人流、电商商品不进 campaign、全响应无 DB id）。T-E2E：Playwright 5/5 有头全绿 + WebM（登录落 personal→切 shop 工作台→切 campaign 复盘，Header/隔离/时间线断言），test_05 一次 timing rerun 后绿。T-QA：`npm run build` 绿、三项安全扫描仅命中断言/内部 ctx 引用。T-UX 补强：Playwright 改为单用例主流程 `test_recording_main_flow`（只登录一次，Header 依次切 personal→shop→campaign→shop），每屏数据加载后默认停留 4s（`E2E_HOLD_MS` 可调）便于录屏观众查看；后端数据加载慢根因定位为 `_fetch`/`_execute` 每次查询新建 psycopg2 连接（远程 DB ~19ms RTT），改为线程本地复用 autocommit 连接（失败自动重连），overview 1.08s→0.16s、summary 1.53s→0.23s（~7x）；全量回归 213 用例绿 |

---

## Phase 1: 基础设施 🔥

| 任务 | 负责人 | 状态 |
| :--- | :--- | :--- |
| 创建 GitHub 仓库 | 项目经理 | ✅ |
| README.md | 项目经理 | ✅ |
| CLAUDE.md | 项目经理 | ✅ |
| AGENTS.md | 项目经理 | ✅ |
| .env.example | 开发组长 | ✅ |
| requirements.txt | 开发组长 | ✅ |
| Dockerfile | 开发组长 | ✅ |
| docker-compose.yml | 开发组长 | ✅ |
| scripts/setup_env.sh | 开发组长 | ✅ |
| scripts/init_db.py | 开发组长 | ✅ |
| 4个空间 JSON 数据 | 开发组长 | ✅ |

## Phase 2: 核心工具

| 任务 | 负责人 | 状态 |
| :--- | :--- | :--- |
| asset_tools.py | 开发组长 | ✅ |
| finance_tools.py | 开发组长 | ✅ |
| human_tools.py | 开发组长 | ✅ |
| knowledge_tools.py | 开发组长 | ✅ |

## Phase 3: Agent & Backend

| 任务 | 负责人 | 状态 |
| :--- | :--- | :--- |
| models/llm_client.py | 项目经理 | ✅ |
| db/postgres_client.py | 项目经理 | ✅ |
| db/chroma_client.py | 项目经理 | ✅ |
| auth/auth.py | 项目经理 | ✅ |
| agents/agent.py | 项目经理 | ✅ |
| app.py | 项目经理 | ✅ |

## Phase 4: AMD GPU 验证

| 任务 | 负责人 | 状态 |
| :--- | :--- | :--- |
| 云资源审批 | 填表Agent | ✅ |
| 一键部署验证 | 项目经理 | ✅ |
| 性能优化 | 项目经理 | ✅ |

## Phase 5: 提交

| 任务 | 负责人 | 状态 |
| :--- | :--- | :--- |
| 演示视频录制 | 全队 | ✅ |
| 项目说明文档 | 开发组长 | ✅ |
| PPT/海报 | 开发组长 | ✅ |
| 最终提交 | 项目经理 | ✅ |
