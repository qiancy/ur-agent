# 类图生成 C# 代码规则

本文档概述了从类图生成 C# 代码的规则。

## 规则：

```csharp
1.  **属性 (Property)：** 所有的 C# 类属性都必须使用 `Property { get; set; }` 语法 。
2.  **Id 属性：** 除泛化结构中的子类外，每个类默认添加一个 `Id` 属性，类型为整型。在泛化结构中，只有最顶层的超类需要默认添加 `Id` 。
3.  **名称属性：** 如果类下面没有任何简单属性，则根据类的名字判断该类是否适合添加一个“名称”属性；如果适合，则给它加上 。
4.  **关联导航：** 在关联关系中，如果没有显式表明导航方向，可以认为由多重性为多（`*` 或 `0..*`）的一方是不可见的，多重性为 1 的一方是可见的 。在生成代码时，多重性为多（`*` 或 `0..*`）的一方有多重性为 1 的一方作为属性，而多重性为 1 的一方的代码中没有和此关联相关的多重性为多（`*` 或 `0..*`）的一方的内容。
5.  **多对多关联：** 针对多对多（`*` 对 `*` 或 `0..*` 对 `0..*`）的关联，默认添加一个中间类。类的名称默认使用关联的名称，如果关联的名称没有标，就使用角色名称并把角色名称的复数形式改为单数形式。标有角色名称的一端默认认为是可见的，未标有角色名称的一端默认认为是不可见的 。因此，中间类中会有可见一端的单值属性，而不可见一端会有中间类的多值属性，默认用 `HashSet<中间类>` 表达 。
6.  **简单属性默认类型：** 如果简单属性的类型未给出，则默认类型为 `string` 。
7.  **现有类型定义：** 作为简单属性的类型出现的内容，如果类图中没有单独给出该类的定义，则不必添加该类的定义 。
8.  **约束 (Constraint)：** 类框的 constraint 一栏的内容是约束，如果你有办法在操作（operation）的代码中体现，可以体现，如果有困难，可跳过不管 。

## 示例：

### 示例 1：基本类生成（规则 1, 2, 3, 4）

给定 PlantUML 类图：
```plantuml
class 协议
class 测量协议
class 源测量协议
class 计算测量协议
class 现象类型
class 观察
class 测量 {
--
_constraints_
{协议.oclIsKindOf(测量协议)}
}
class 计算测量 {
--
_constraints_
{协议.oclIsKindOf(计算测量协议)}
}
class 关注对象
class 数量
测量协议     --|> 协议
源测量协议   --|> 测量协议
计算测量协议 --|> 测量协议
测量   --|> 观察
计算测量 --|> 测量
协议      "1"     -- "0..*"        观察
关注对象  "1"     -- "0..*"        观察
数量      "1" -- "0..*"        测量
测量协议  "0..*"  -- "1"           现象类型
测量  "0..*"  -- "1"           现象类型 : "/通过测量协议派生"
计算测量  "0..*"  -- "0..*\n源测量s" 测量
生成的 协议、测量协议 和 现象类型 类的 C# 代码:
```csharp
public class 协议
{
    public int Id { get; set; } // 规则 2 
    public string 名称 { get; set; } // 规则 3 
}

public class 测量协议 : 协议
{
    public 现象类型 现象类型 { get; set; } // 规则 4 
}

public class 现象类型
{
    public int Id { get; set; } // 规则 2 
    public string 名称 { get; set; } // 规则 3 
}
协议 默认增加了 Id ，并配一个“名称”属性，使用了 Property 。
现象类型 默认增加了 Id ，并配一个“名称”属性，使用了 Property 。
测量协议 继承 协议，但由于是子类，不需要加 Id 。
由于 测量协议 和 现象类型 的关联中，测量协议 的多重性为 0..*，现象类型 的多重性为 1，根据规则 4，测量协议 中有 现象类型 Property，而 现象类型 中没有 测量协议 Property 。
```
### 示例 2：多对多关联（规则 5）
考虑 PlantUML 类图中 计算测量 和 测量 之间的关联，其多重性为 0..* 对 0..*，因此默认添加一个中间类 。
生成的 源测量、测量 和 计算测量 的 C# 代码:
```csharp
public class 源测量 // 中间类，名称来源于角色名“源测量s” 
{
    public int Id { get; set; } // 规则 2 
    public 测量 测量 { get; set; } // 可见端，单值属性 
}

public class 测量 : 观察
{
    public 数量 数量 { get; set; } // 规则 4 
}

public class 计算测量 : 测量
{
    public HashSet<源测量> 源测量s { get; set; } // 不可见端，多值属性 
}
由于关联没有命名，所以该中间类用角色名“源测量”命名（用单数“源测量”而不是复数“源测量s”） 。
关联的“测量”一端标了角色名“源测量s”，说明是可见的，因此，“源测量”中会有“测量”的单值属性 。
关联的“计算测量”一端未标角色名，说明不可见，因此，“计算测量”中会有 源测量s 的 HashSet<源测量>，注意是 HashSet<源测量> 而不是 HashSet<测量> 。
在 测量 中，不需要有 源测量 的属性 。
```
### 示例 3：简单属性（规则 6, 7）
给定 PlantUML 类图:
```csharp
class 账户 {
  - 编号
  - 余额: 数量
  --
  _constraints_
  {余额 = 分录s->sum(e | e.数额)}
}
class 分录 {
  - 数额: 数量
  - 发生时间: 时间点
  - 记账时间: 时间点
}
账户 "1" -- "0..* \n分录s " 分录
生成的 C# 代码:
```csharp
public class 账户
{
    public int Id { get; set; } // 规则 2 
    public string 编号 { get; set; } // 规则 6 
    public 数量 余额 { get; set; }
    public HashSet<分录> 分录s { get; set; }
}

public class 分录
{
    public int Id { get; set; } // 规则 2 
    public 数量 数额 { get; set; }
    public 时间点 发生时间 { get; set; } 
    public 时间点 记账时间 { get; set; } 
}
Id 属性根据规则 2 添加到 账户 和 分录 。
账户 具有 数量 类型的 余额 属性和由于 1 对 0..* 关联而生成的 HashSet<分录> 类型的 分录s 属性。
分录 具有 数量 类型的 数额 属性，以及 时间点 类型的 发生时间 和 记账时间 属性。
虽然当前代码不存在 数量 和 时间点 类，但根据规则 7不需要添加该类 。

```