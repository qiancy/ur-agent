# BE-04 收尾开发实现报告（待 PM Review）

> 开发团队交付报告，按 `agents/pm/BE-04_收尾开发安排.md` §7 格式整理。
> 日期：2026-08-02
> 状态：待 PM review（PM 红线通过后允许合并）
> 更新：FINAL-SECURITY（PM 评审指令）已落实并附最终回归与扫描结果。

## 0. FINAL-SECURITY 变更摘要（PM 评审指令）

PM 评审结论：BE-04 Seller 范围可合并，但需先完成 FINAL-SECURITY 清理方可作为比赛交付前安全基线。四项指令全部落实：

| # | PM 指令 | 实现 |
| :--- | :--- | :--- |
| 1 | `/seller` 全部接口统一 ecommerce 校验 | `src/routers/deps.py` 新增 `require_ecommerce_context`；`src/routers/seller.py` 7 个端点（含 GET stock/movements/summary/product-summary + 写入 + chat）全部改为该校验，非 ecommerce 返回 403 |
| 2 | 移除「交易 #id」展示 | `src/frontend.py:486` 交易标题去掉 `#{txn['id']}` |
| 3a | `/party` 的 `transaction_id` → `transaction_uid` 业务键 | 交易表新增 `transaction_uid UNIQUE NOT NULL`（幂等迁移 + 存量回填）；`GET /transaction` 返回业务字段（去 `id`、parties 去 `person_id`）；`POST /party` 改收 `transaction_uid`（组织内 404 校验）；`GET /party/transaction/{transaction_uid}` 改 uid + org 鉴权；前端 + 3 个测试文件同步迁移 |
| 3b | 战役 replay/delete 改按 `campaign_code` 定位 | `GET /campaigns/imports` 去 `id`/嵌套 org `id`；`GET /imports/{code}/replay`、`DELETE /imports/{code}` 按 `campaign_code`（active 唯一）定位，响应去 `campaign_import_id`；前端批次 ID 输入改为「战役代号」文本，列表去 `#id` |

## 1. 变更文件清单

| 文件 | 变更 | 说明 |
| :--- | :--- | :--- |
| `src/routers/deps.py` | 修改 | 新增 `require_ecommerce_context(request)`（包装 strict org context，非 ecommerce 抛 403） |
| `src/routers/seller.py` | 修改 | 7 个端点统一改用 ecommerce 校验；chat 内联 org_type 校验并入 helper；移除多余 import |
| `src/db/database.py` | 修改 | transaction 表加 `transaction_uid`；幂等迁移（ADD COLUMN IF NOT EXISTS + 存量回填 + NOT NULL + UNIQUE 约束）；`create_transaction`/2 处内联交易 INSERT 生成 uid；`get_transactions` 返回业务字段（去 `t.*` 的 id、parties 去 `person_id`）；新增 `get_transaction_by_uid`；`query_party_by_transaction` 改 uid + org 范围 + 去 id；`list_campaign_imports_for_orgs` 去 `ci.*` 的 id 与嵌套 org id |
| `src/models/schemas.py` | 修改 | `PartyCreate.transaction_id: int` → `transaction_uid: str` |
| `src/routers/party.py` | 修改 | POST 改按 uid 解析交易（404）；`GET /party/transaction/{transaction_uid}` 改 uid + org 鉴权 |
| `src/routers/campaign.py` | 修改 | replay/delete 路径参数改 `campaign_code`；replay 响应去 `campaign_import_id`；移除未用 import |
| `src/frontend.py` | 修改 | `:486` 去「交易 #id」；`:514/523` 用 `transaction_uid`；战役列表去 `#id`；replay/delete 输入改「战役代号」文本；导入提示去 `#id`；`:478` person_id 回退 →「未知人员」（上一轮） |
| `agents/tdd/test_seller_chat_api.py` | 修改 | 新增 `test_seller_read_endpoints_reject_non_ecommerce_org`（GET 端点对非 ecommerce token 断言 403） |
| `agents/tdd/test_three_kingdoms_http.py` | 修改 | 交易/party 断言与请求体迁移至 `transaction_uid`；`/party/transaction/{uid}` 用例改先建交易再查 |
| `agents/tdd/test_fire_newye_api.py` | 修改 | `create_party` 参数改 `transaction_uid` |
| `agents/tdd/setup_fire_newye_campaign.py` | 修改 | 同上 |
| `agents/tdd/test_be02_acceptance.py` | 修改 | 战役删除用例改按 `campaign_code` |

复核清单（未改动，确认为合规现状）：

| 文件 | 复核结论 |
| :--- | :--- |
| `src/tools/seller_tools.py` | ✅ 仅 4 个只读工具；签名无身份/`*_id`；输出无 DB 主键 |
| `src/agents/seller_agent.py` | ✅ 仅接受显式工具列表；本地 prompt；无 `hub.pull` |
| `src/db/database.py`（Seller 4 查询） | ✅ 仅返回业务字段 |

## 2. Seller 前端可见性说明（无 Gradio E2E，代码层复核）

- `_show_workspace(state)`：`seller_panel` 仅当 `org_type == "ecommerce"` 时 `visible=True`；非 ecommerce 数据区留空。
- **ecommerce 账号**：显示 Seller tabs（库存 / 流水 / 摘要 / AI）。
- **三国 / company / system 组织**：不显示 Seller tabs。
- 本轮追加：即使绕过前端直接调用 `/seller/*` GET，非 ecommerce 组织亦返回 403（PM 指令 1）。

## 3. Seller AI 工具集合清单

```
seller_stock
seller_summary
seller_product_summary
seller_inventory_movements
```

- 全部只读；`make_seller_tools()` 以闭包绑定 `shop_key`，不对模型暴露。
- 写工具（purchase-in/sales-out 走 `/seller/*` 写入 API）不入 AI 工具集。

## 4. 验收命令输出摘要

| 命令 | 结果 |
| :--- | :--- |
| `python3 -m compileall src agents/tdd` | ✅ 通过 |
| `python3 -m pytest agents/tdd/test_seller_ai_tools.py agents/tdd/test_seller_chat_api.py -q` | ✅ **27 passed**（9 + 18，含新增 ecommerce 403 用例）in 120.60s |
| `python3 -m pytest agents/tdd/test_seller_inventory_transaction_api.py agents/tdd/test_seller_summary_api.py agents/tdd/test_be02_acceptance.py -q` | ✅ **45 passed** in 239.55s |
| `python3 -m pytest agents/tdd/test_three_kingdoms_http.py::TestTransactionAPI ::TestPartyAPI ::TestSummaryAPI -q` | ✅ **11 passed** in 6.87s（3a 迁移回归） |
| `git diff --check` | ✅ 无 whitespace 错误 |

迁移验证（增量手动 API 契约检查）：

- `GET /transaction`：返回 `transaction_uid`，无 `id`；parties 无 `person_id`。
- `POST /transaction` 返回 `transaction_uid`；`POST /party` 收 `transaction_uid`，未知 uid 返回 404。
- `GET /party/transaction/{uid}`：按 uid + org 查询，无 DB id。
- `GET /campaigns/imports`：仅业务字段，组织对象无 `id`。
- `GET /imports/{code}/replay`、`DELETE /imports/{code}`：按 `campaign_code` 定位，响应无 `campaign_import_id`。

## 5. 四项安全扫描结果（FINAL 复跑）

### 5.1 明文密钥扫描（BE-04 §5.1）

```
rg -n "unires_secure_pass_2024|unires-dev-jwt-secret|rc-[A-Za-z0-9]{20,}" .
```

- 命中 **2 处**，均为文档中扫描命令自身的正则文本（自引用）：`agents/pm/BE-04_收尾开发安排.md:122`、`agents/tdd/BE-04_收尾实现报告.md:58`，**非真实密钥**。
- 结论：**无真实密钥入库**。`.env` 含真实口令/密钥，已被 `.gitignore` 忽略。

### 5.2 `puid` / `ouid` 旧身份字段扫描（BE-04 §5.2）

```
rg -n '"pid"|"oid"|pid=|oid=' src agents/tdd docs README.md
```

- 命中 **1 处**：`agents/tdd/test_seller_ai_tools.py:276` —— 测试禁止字段集合 `{"puid", "ouid", "pid", "oid"}`（§5.2 验收「测试中的禁止字段集合可人工 review 放行」）。
- 结论：`pid`/`oid` 旧键在业务数据流中 **零命中**；本轮迁移未重新引入旧键。

### 5.3 DB 数字 ID 对外响应扫描（BE-04 §5.3）

```
rg -n '"id"|[A-Za-z_]+_id|organization_id|person_id|resource_id|warehouse_id|transaction_id|inventory_movement_id' \
  src/routers/seller.py src/tools/seller_tools.py src/agents/seller_agent.py \
  src/frontend.py agents/tdd/test_seller_ai_tools.py agents/tdd/test_seller_chat_api.py
```

| 文件 | 命中分析 | 放行 |
| :--- | :--- | :--- |
| `src/tools/seller_tools.py` | 零命中 | ✅ |
| `src/agents/seller_agent.py` | 零命中 | ✅ |
| `src/routers/seller.py` | 仅 `ctx["organization_id"]` 等内部闭包变量 + `_reject_identity_params` 拒绝规则 | ✅ 服务端内部变量可放行 |
| `src/frontend.py` | **零命中**（上一轮 `:478` 已修；本轮 `transaction_id`、`#id`、战役批次 ID 全部移除） | ✅ |
| 两个测试文件 | `_DB_ID_FIELDS` 禁止字段集合、`_assert_no_db_ids` 辅助、mock `call_id` | ✅ 测试禁止字段集合可放行 |

- 结论：**Seller 与一般工作区的前端展示/请求体、交易/party/战役 API 响应均无 DB 数字 ID**（FINAL-SECURITY 3a/3b 已闭环，§6 第 1 项已解决）。

### 5.4 默认测试不触发真 LLM（BE-04 §5.4）

| 命令 | 结果 |
| :--- | :--- |
| `env -u LLM_API_KEY -u JWT_SECRET python3 -m pytest agents/tdd/test_seller_chat_live_llm.py -q` | ✅ **4 deselected** in 0.23s（integration marker 默认排除） |
| `env -u LLM_API_KEY -u JWT_SECRET python3 -m pytest agents/tdd/test_seller_chat_api.py -q` | ✅ **18 passed** in 76.57s（仅 mock LLM） |

- 结论：默认测试零真 LLM 调用；真 LLM 仅允许 `pytest -m integration` 手动执行。

## 6. 已知问题和未做范围

1. **`agents/tdd/test_three_kingdoms_http.py` 既有数据污染（非本轮引入）**：`TestResourceAPI` 4 例（测试物资/资金/人力/知识）因共享测试库中 `shu` 组织已存在同名资源报 `uq_resource_org_name` 唯一冲突，属历史用例不清理自身数据的既有问题，与本轮迁移无关（交易/party 相关用例全部通过）。
2. **`TestChatAPI::test_chat_with_context` 504**：调用真实 LLM 超时（Gateway Timeout），为环境性失败（LLM 服务不可达/慢），非代码缺陷；`/chat` 不在本轮改动范围。
3. **无 Gradio E2E**：按计划要求，前端以代码复核替代截图，未做 E2E。
4. **未提交**：全部 BE-04 变更保留在工作区，待 PM review 后合并；提交范围仅含 BE-04 相关文件，排除 `.env`、`docker-compose.yml`、`agents/sa/4-modeling/*.html` 等无关变更。
